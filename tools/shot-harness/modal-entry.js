/**
 * Re-exports the real modal for the preview harness, so what is inspected is
 * the shipped module rather than a copy of it.
 */
export { openScreenshotModal } from '/src/options/screenshot-modal.js'
